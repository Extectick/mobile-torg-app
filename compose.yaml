services:
  proxy:
    image: nginx:1.27-alpine
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-3000}:80"
    volumes:
      - ./docker/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./logs/nginx:/var/log/nginx
    depends_on:
      - web

  migrate:
    profiles:
      - tools
    build:
      context: .
      target: migrator
    environment:
      DATABASE_URL: ${DATABASE_URL}
      LOG_PRISMA_QUERIES: ${LOG_PRISMA_QUERIES:-false}
    restart: "no"

  web:
    build:
      context: .
      target: runner
    restart: unless-stopped
    environment:
      DATABASE_URL: ${DATABASE_URL}
      LOG_HTTP_REQUESTS: ${LOG_HTTP_REQUESTS:-true}
      LOG_PRISMA_QUERIES: ${LOG_PRISMA_QUERIES:-false}
      ERROR_IMAGES_DIR: /app/logs/error-images
      NODE_ENV: production
    volumes:
      - ./logs/app:/app/logs
      - ./logs/error-images:/app/logs/error-images
    expose:
      - "3000"
